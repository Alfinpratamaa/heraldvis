[package]
name = "heraldvis-core"
version = "0.1.0"
edition = "2021"
description = "Tipe bersama, schema tool-call, error types (Heraldvis)"
license = "MIT"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }

[lints]
workspace = true
